apiVersion: v1
kind: Service
metadata:
  name: fte-api-service
  labels:
    app: fte-api
    component: api
spec:
  type: ClusterIP
  selector:
    app: fte-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP